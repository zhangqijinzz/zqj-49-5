**0-1代码生成**

1. 【一般】当前首页只展示了本周统计概览，但用户缺少一个直观的居住环境评分来判断噪音趋势变化。需要新增一个噪音健康分卡片，根据近7天的事件次数、总受扰时长和夜间打扰占比综合算出0-100的评分，附带与上周的对比变化箭头和一句简短改善建议，底层数据变动时评分自动刷新。

2. 【困难】租房维权时常要把噪音记录分享给物业或房东，可导出HTML只能自己打印，对方没法方便查看。需要新增报告分享功能，把选定时段的统计摘要和事件列表编码为携带参数的URL，对方打开链接即可在浏览器查看报告页面。需处理URL长度超限时的参数压缩或分片兼容，同时为分享链接设置有效期，过期后打开提示对方重新生成。

3. 【一般】所有记录存在浏览器本地，换设备或误清缓存就会丢失。需要增加数据备份与恢复功能，支持把全部记录和证据元数据一键导出为单个JSON文件，也可从备份文件恢复数据。恢复时遇到ID冲突需做覆盖或跳过处理，完成后展示成功导入了多少条记录和证据、跳过了多少条重复数据。

4. 【困难】现在一条记录只能设一个开始和结束时间，但噪音经常间歇性反复发生，单段时长无法准确描述。需要让一条记录支持多个不连续的时间段，在表单里提供时间段列表编辑器，用户可增删改每个时段的起止时间，系统自动累加总时长。已有单段时长记录需自动兼容，所有涉及时长计算的列表页、统计页和导出报告逻辑都要更新为累加模式。

5. 【困难】同一类噪音经常反复出现，但每次都要从头填一遍相似内容。需要新增记录模板功能，用户可把一条现有记录存为模板（保留噪音类型、位置、强度、影响标签和描述，清空具体时间日期），之后从模板新建时自动填充这些常用字段，只调整时间和备注就能快速提交。模板需有独立管理列表，支持编辑、重命名、排序和删除。

6. 【一般】向物业投诉时常需要展示投诉前后的噪音变化，但当前没有直观对比手段。需要新增时段对比视图，让用户选择两个时间范围（如投诉前一周和投诉后一周），并排展示事件次数、平均强度、夜间打扰比例和影响标签分布的核心指标差异，用对比柱状图呈现变化，方便在维权沟通中作为客观论据。

7. 【困难】按日期列表翻看日志不够直观，用户难以快速定位到某一天或看出整月噪音密度。需要新增月历视图，以月历网格展示每天事件数量并用色块深浅反映密度高低，有夜间噪音的日期加特殊标记。点击某天可展开当天事件摘要浮层或跳转到该天日志页，切换月份时从现有数据源实时读取，保持与记录列表同步。

8. 【困难】数据只存在一台设备本地，换电脑或在手机上看不到。需要新增局域网同步能力，让同一网络内的两台设备自动发现对方，可选择单向或双向同步记录数据。同步过程需处理冲突合并（以最新修改时间为准）、增量传输（只同步新增或有变更的记录）和进度反馈，完成后展示同步结果摘要。

**Feature迭代**

9. 【困难】首页统计卡片固定展示本周数据，可用户有时想看上周或近30天的趋势只能去导出页。需要让首页统计卡片支持点击切换时间范围（本周、上周、近30天），切换后四张统计卡片的数值、环比箭头、趋势图和标签分布图都同步更新。环比基准需根据所选范围正确调整，不能固定对比上一周。

10. 【一般】导出报告目前自定义日期范围后没有清晰的天数提示和快捷入口，每次要手动输入起止日期。希望在自定义模式下显示共X天的统计摘要，在日期输入附近增加最近7天、最近30天、本周等快捷按钮，一键填入对应范围，减少手动操作和日期填错的可能。

11. 【困难】用户上传图片证据后只能原样展示，维权时需要在图片上标注噪音位置或添加时间水印才更有说服力。需要扩展上传流程，上传图片后可进入编辑模式，添加时间戳水印、矩形标注框和文字说明，生成带标注的新版本图片保存为证据，同时保留原始图片。编辑过程支持撤销操作，生成的标注图片在详情页和导出报告中同步展示。

12. 【一般】日志列表同一天内记录全混在一起，记录多了很难按时间段快速定位。需要按凌晨、上午、下午、傍晚、夜间对同一天记录做二级分组，每个时段头部分组显示该时段的记录条数和总时长，无记录的时段自动隐藏。时段组支持点击折叠展开，默认展开有数据的时段，折叠状态在页面内保持。

13. 【困难】删除记录是永久性的，误删后无法找回，对积累了大量证据的用户风险很高。需要引入回收站机制：删除的记录先进入回收站而非直接移除，保留30天后自动清空。回收站内记录不计入任何统计，用户可在回收站查看已删除记录摘要，支持批量恢复和永久删除。恢复后的记录重新参与统计，关联证据在回收期间一并保留。

14. 【困难】每次生成报告都要重新配置时间范围和维度，想回顾之前生成的报告只能重新配置一遍。需要新增报告快照功能，用户在报告预览后可一键保存为快照，记录当时的统计结果和报告内容，在独立列表中展示，支持查看详情、重命名和删除。如果原始数据在快照保存后有大量变更，查看快照时需提示用户数据可能已过期。

15. 【困难】日志页的筛选条件离开页面或刷新后就丢了，每次回来要重新勾选噪音类型和影响标签。需要让筛选条件自动记忆最近一次选择，同时支持把常用筛选组合保存为命名方案（如夜间装修噪音），筛选栏增加方案选择器一键切换。保存的方案需支持编辑、重排序和删除，切换方案时筛选栏状态和记录列表同步更新。

16. 【困难】编辑记录时只能整体替换描述内容，如果想补充一段新情况或追加一张图片，必须进入编辑模式全量修改完一起保存。希望详情页的描述区和证据区各自增加追加入口，追加的文字自动带时间戳标记，追加的图片也标记上传时间。追加内容在详情页按时间倒序展示，在日志列表的记录摘要上提示有补充内容。

**代码理解**

17. 【困难】需要梳理从新增记录表单提交到最终展现在首页统计卡片和趋势图上的完整数据链路，包括表单校验流程、store状态更新时机、localStorage持久化触发节点、各统计指标的重新计算入口、图表数据准备步骤和组件重新渲染的触发链。最终产出README文档，聚焦回答以下问题：各统计指标分别在哪个环节被重新计算？筛选条件变化时日志列表和首页统计面板的刷新路径有何不同？证据的增删怎样反向更新关联记录的updatedAt字段？
